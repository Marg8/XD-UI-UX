import 'package:flutter/material.dart';

class Addnewsaving extends StatelessWidget {
  Addnewsaving({
    Key? key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xffffffff),
      body: Stack(
        children: <Widget>[],
      ),
    );
  }
}
